# 🤖 Generated wholly or partially with GPT-5.6 Sol; OpenAI Codex
name: Build for Android

on:
  # Enable manual run
  workflow_dispatch:
  push:
    tags:
      - "**"
    branches:
      - "**"
    paths:
      - .github/workflows/android.yml

jobs:
  warmup-android-cache:
    # We can't cache the signed builds for security reasons,
    # so run a developer-signed debug build to populate the cache.
    name: Build debug APK for caching (developer signing)
    runs-on: ubuntu-latest
    # Only run on the main branch to save cache space,
    # since all other branches can use the main branch's cache.
    if: github.ref_name == 'main'
    timeout-minutes: 60
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Check if Android cache exists
        id: cache
        uses: actions/cache/restore@v6.1.0
        with:
          lookup-only: true
          path: |
            .dart_tool/
            **/.gradle/
            **/.idea/
            **/.kotlin/
            **/GeneratedPluginRegistrant.*
            **/android/local.properties
            **/*.iml
            build/
            packages/**/.dart_tool/
            packages/**/.flutter-plugins-dependencies
            packages/**/pubspec.lock
            submodules/flutter/bin/cache/
            submodules/flutter/packages/flutter_tools/gradle/build/
          key: ${{ runner.OS }}-saber_android

      - name: Restore platform-agnostic cache
        uses: actions/cache@v6.1.0
        if: ${{ steps.cache.outputs.cache-hit == '' }}
        with:
          path: |
            .dart_tool/
            .flutter-plugins-dependencies
            build/
            linux/flutter/ephemeral/
            macos/Flutter/ephemeral/
            packages/**/.dart_tool/
            packages/**/.flutter-plugins-dependencies
            packages/**/pubspec.lock
            windows/flutter/ephemeral/
            ~/.dartServer/
            ~/.dart-tool/
          key: ${{ runner.OS }}-saber-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: |
            ${{ runner.OS }}-saber-

      - uses: "./.github/shared_workflows/install_apt_dependencies"
        if: ${{ steps.cache.outputs.cache-hit == '' }}

      - name: Setup Flutter
        uses: adil192/setup-flutter-submodule@v3
        if: ${{ steps.cache.outputs.cache-hit == '' }}
        with:
          flutter-path: submodules/flutter

      - name: Setup Java
        uses: actions/setup-java@v5.6.0
        if: ${{ steps.cache.outputs.cache-hit == '' }}
        with:
          distribution: "microsoft"
          java-version: "21"
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        if: ${{ steps.cache.outputs.cache-hit == '' }}
        with:
          cache-write-only: true

      - name: Setup Rust
        uses: moonrepo/setup-rust@v1
        if: ${{ steps.cache.outputs.cache-hit == '' }}
        with:
          # The rust version is read from rust-toolchain.toml
          targets: armv7-linux-androideabi,aarch64-linux-android,x86_64-linux-android

      - name: Disable Flutter CLI animations
        run: flutter config --no-cli-animations
        if: ${{ steps.cache.outputs.cache-hit == '' }}

      - run: ./patches/pre/remove_dev_dependencies.sh
        if: ${{ steps.cache.outputs.cache-hit == '' }}
      - run: ./patches/pre/remove_linux_dependencies.sh
        if: ${{ steps.cache.outputs.cache-hit == '' }}
      - run: flutter pub get
        if: ${{ steps.cache.outputs.cache-hit == '' }}
      - run: ./patches/post/patch_rust_versions.sh
        if: ${{ steps.cache.outputs.cache-hit == '' }}

      - name: Build debug apk
        run: flutter build apk --debug
        if: ${{ steps.cache.outputs.cache-hit == '' }}

      - name: Save Android cache
        uses: actions/cache/save@v6.1.0
        if: ${{ steps.cache.outputs.cache-hit == '' }}
        with:
          path: |
            .dart_tool/
            android/.gradle/
            android/.kotlin/
            build/

            submodules/flutter/bin/cache/
            submodules/flutter/packages/flutter_tools/gradle/.gradle/
            submodules/flutter/packages/flutter_tools/gradle/.kotlin/
            submodules/flutter/packages/flutter_tools/gradle/build/
          key: ${{ steps.cache.outputs.cache-primary-key }}

  build-android:
    name: Build APK (${{ matrix.foss && 'FOSS' || 'Regular' }})
    strategy:
      fail-fast: false
      matrix:
        foss: [false, true]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 60
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - uses: "./.github/shared_workflows/install_apt_dependencies"

      - name: Setup Flutter
        uses: adil192/setup-flutter-submodule@v3
        with:
          flutter-path: submodules/flutter
          no-save-caches: true

      - name: Restore platform-agnostic cache
        uses: actions/cache/restore@v6.1.0
        with:
          path: |
            .dart_tool/
            .flutter-plugins-dependencies
            build/
            linux/flutter/ephemeral/
            macos/Flutter/ephemeral/
            packages/**/.dart_tool/
            packages/**/.flutter-plugins-dependencies
            packages/**/pubspec.lock
            windows/flutter/ephemeral/
            ~/.dartServer/
            ~/.dart-tool/
          key: ${{ runner.OS }}-saber-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: |
            ${{ runner.OS }}-saber-

      - name: Restore Android cache
        uses: actions/cache/restore@v6.1.0
        with:
          path: |
            .dart_tool/
            **/.gradle/
            **/.idea/
            **/.kotlin/
            **/GeneratedPluginRegistrant.*
            **/android/local.properties
            **/*.iml
            build/
            packages/**/.dart_tool/
            packages/**/.flutter-plugins-dependencies
            packages/**/pubspec.lock
            submodules/flutter/bin/cache/
            submodules/flutter/packages/flutter_tools/gradle/build/
          key: ${{ runner.OS }}-saber_android

      - name: Setup release keystore
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
          KEY_STORE_PASSWORD: ${{ secrets.KEY_STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          ALIAS: ${{ secrets.ALIAS }}
        run: |
          signing_values=(SIGNING_KEY KEY_STORE_PASSWORD KEY_PASSWORD ALIAS)
          supplied=0
          for name in "${signing_values[@]}"; do
            if [ -n "${!name}" ]; then
              supplied=$((supplied + 1))
            fi
          done

          if [ "$supplied" -ne 0 ] && [ "$supplied" -ne "${#signing_values[@]}" ]; then
            echo "Configured Android signing secrets must either all be set or all be unset." >&2
            exit 1
          fi

          if [ "$supplied" -eq "${#signing_values[@]}" ]; then
            printf '%s' "$SIGNING_KEY" | base64 --decode > android/android.keystore
            signing_provenance=owner
          else
            KEY_STORE_PASSWORD=$(openssl rand -hex 32)
            KEY_PASSWORD="$KEY_STORE_PASSWORD"
            ALIAS=saber-community
            keytool -genkeypair -noprompt \
              -keystore android/android.keystore \
              -storepass "$KEY_STORE_PASSWORD" \
              -keypass "$KEY_PASSWORD" \
              -alias "$ALIAS" \
              -keyalg RSA \
              -keysize 4096 \
              -validity 10000 \
              -dname "CN=Saber Community CI"
            signing_provenance=community
          fi

          chmod 600 android/android.keystore
          {
            echo "storePassword=$KEY_STORE_PASSWORD"
            echo "keyPassword=$KEY_PASSWORD"
            echo "keyAlias=$ALIAS"
            echo "storeFile=../android.keystore"
            echo "signingProvenance=$signing_provenance"
          } > android/key.properties

      - name: Setup Java
        uses: actions/setup-java@v5.6.0
        with:
          distribution: "microsoft"
          java-version: "21"
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-read-only: true

      - name: Setup Rust
        uses: moonrepo/setup-rust@v1
        with:
          # The rust version is read from rust-toolchain.toml
          targets: armv7-linux-androideabi,aarch64-linux-android,x86_64-linux-android

      - name: Disable Flutter CLI animations
        run: flutter config --no-cli-animations

      - run: ./patches/pre/remove_dev_dependencies.sh
      - run: ./patches/pre/remove_linux_dependencies.sh
      - run: ./patches/pre/remove_proprietary_dependencies.sh
        if: matrix.foss
      - run: flutter pub get
      - run: ./patches/post/patch_rust_versions.sh

      - name: Build apk
        run: flutter build apk

      - name: Rename apk
        id: rename_apk
        run: |
          buildName=$(grep -oP "(?<=buildName = ').*(?=')" lib/data/version.dart)
          if [ "${{ matrix.foss }}" = "true" ]; then
            apkName="Saber_FOSS_v${buildName}.apk"
          else
            apkName="Saber_v${buildName}.apk"
          fi
          echo "apkName=$apkName" >> $GITHUB_OUTPUT

          mkdir -p output
          releaseApks=(build/app/outputs/flutter-apk/app-release*.apk)
          if [ "${#releaseApks[@]}" -ne 1 ] || [ ! -f "${releaseApks[0]}" ]; then
            echo "Expected exactly one release APK, found ${#releaseApks[@]}." >&2
            exit 1
          fi
          mv "${releaseApks[0]}" "output/$apkName"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.foss && 'Saber-Android-FOSS' || 'Saber-Android' }}
          path: output/${{ steps.rename_apk.outputs.apkName }}

      - name: Upload to GitHub release
        uses: svenstaro/upload-release-action@v2
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: output/${{ steps.rename_apk.outputs.apkName }}

      - name: Build aab
        if: ${{ matrix.foss == false }}
        run: |
          flutter build appbundle --no-pub \
            --dart-define=FLAVOR="Google Play" \
            --dart-define=APP_STORE="Google Play" \
            --dart-define=UPDATE_CHECK="false"

      - name: Rename aab
        if: ${{ matrix.foss == false }}
        run: |
          mkdir -p output
          mv build/app/outputs/bundle/release/app-release.aab output/Saber.aab

      - name: Upload artifact
        if: ${{ matrix.foss == false }}
        uses: actions/upload-artifact@v7
        with:
          name: Saber-Android-PlayStore
          path: output/Saber.aab

  upload-to-play-store:
    name: Upload to Play Store
    runs-on: ubuntu-latest
    needs: build-android
    if: ${{ startsWith(github.ref, 'refs/tags/') }}
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Download artifact
        uses: actions/download-artifact@v8
        with:
          name: Saber-Android-PlayStore
          path: output

      - name: Prepare whatsnew directory from metadata/*/changelogs
        working-directory: ${{ github.workspace }}
        run: |
          # display changelogs for debugging
          find metadata -name '*.txt'

          mkdir -p whatsnew

          # get current version code from lib/data/version.dart
          VERSION_CODE=$(grep -oP '(?<=buildNumber = )\d+' lib/data/version.dart)

          # copy changelogs of each language to whatsnew
          for dir in metadata/*/changelogs; do
              # get language code from directory name
              # e.g. metadata/en-US/changelogs -> en-US
              lang=${dir#metadata/}
              lang=${lang%/changelogs}

              # get original changelog file
              original="$dir/$VERSION_CODE.txt"

              if [ -f "$original" ]; then
                # copy changelog file to whatsnew
                cp "$original" "whatsnew/whatsnew-${lang}"
              fi
          done

      - name: Upload to Play Store
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_STORE_JSON }}
          packageName: com.adilhanney.saber
          releaseFiles: output/Saber.aab
          track: production
          status: completed
          whatsNewDirectory: whatsnew
