/// 🤖 Generated wholly or partially with GPT-5.6 Sol; OpenAI
library;

/// Installer execution is unavailable outside native builds.
const bool isAvailable = false;

/// Rejects installer downloads on unsupported platforms.
Future<void> downloadAndInstall(
  Uri downloadUri, {
  void Function(double)? onProgress,
}) => throw UnsupportedError('Windows installer updates are unavailable');
